# Railway Environment Variables Configuration
# Copy these to your Railway service's environment variables section

# =============================================================================
# MongoDB Configuration
# =============================================================================
# Use MongoDB Atlas connection string (recommended for Railway)
# Format: mongodb+srv://username:password@cluster.mongodb.net/database?retryWrites=true&w=majority
MONGODB_URL=mongodb+srv://docextract_user:YOUR_PASSWORD_HERE@docextract-cluster.xxxxx.mongodb.net/docextract?retryWrites=true&w=majority

# Database name (should match the one in MONGODB_URL)
MONGODB_DB_NAME=docextract

# =============================================================================
# LlamaParse AI Configuration
# =============================================================================
# Get your API key from: https://cloud.llamaindex.ai
LLAMA_CLOUD_API_KEY=llx-your-llamaparse-api-key-here

# =============================================================================
# Server Configuration
# =============================================================================
# Host (Railway requires 0.0.0.0 to accept external connections)
HOST=0.0.0.0

# Port (Railway will provide this automatically via $PORT variable)
# Default: 8000
PORT=8000

# =============================================================================
# CORS Configuration
# =============================================================================
# Comma-separated list of allowed origins
# Update this with your actual frontend URLs after deployment
# Examples:
# - Railway frontend: https://docextract-frontend-production.up.railway.app
# - Custom domain: https://app.yourdomain.com
# - Local development: http://localhost:3000
ALLOWED_ORIGINS=https://your-frontend-url.up.railway.app,https://app.yourdomain.com,http://localhost:3000

# =============================================================================
# Optional: Application Settings
# =============================================================================
# Log level: debug, info, warning, error, critical
LOG_LEVEL=info

# Environment: development, staging, production
ENVIRONMENT=production

# =============================================================================
# Railway-Specific Notes
# =============================================================================
# 1. Never commit this file with actual secrets to git
# 2. Set these variables in Railway dashboard under "Variables" tab
# 3. Railway automatically provides: $PORT, $RAILWAY_ENVIRONMENT, $RAILWAY_SERVICE_NAME
# 4. MongoDB Atlas is recommended over Railway's built-in database for MongoDB
# 5. Enable "Restart on failure" in Railway deployment settings
# 6. Set up health checks at /health endpoint

# =============================================================================
# Quick Setup Guide
# =============================================================================
# 1. Create MongoDB Atlas cluster (free tier available)
# 2. Get LlamaParse API key
# 3. Copy all variables above to Railway → Your Service → Variables
# 4. Replace placeholder values with actual credentials
# 5. Deploy!

# =============================================================================
# Security Checklist
# =============================================================================
# ✓ Use strong MongoDB passwords (min 16 characters, mixed case, numbers, symbols)
# ✓ Enable MongoDB IP whitelist (0.0.0.0/0 for Railway, or specific IPs)
# ✓ Never expose API keys in client-side code
# ✓ Use environment variables for all secrets
# ✓ Enable MongoDB Atlas encryption at rest
# ✓ Regularly rotate API keys
# ✓ Restrict CORS to specific domains in production
